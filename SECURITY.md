# 安全策略

## 支持范围

当前维护主分支 `main` 以及最新发布版本。安全修复会优先进入 `main`，随后按需要发布新版本。

## 报告安全问题

请不要在公开 issue 中披露未修复的安全漏洞。

建议通过私有渠道联系维护者，并提供：

- 受影响的版本或提交
- 复现步骤
- 影响范围
- 可行的修复建议

## 依赖安全

仓库提供安全审计脚本：

```bash
pnpm audit:security
```

CI 会在 PR 和 `main` 分支推送时运行 `pnpm run check`，其中包含依赖安全审计。审计使用 npm 官方 registry，避免镜像源缺少 audit endpoint 导致误报失败。
